/* You can add global styles to this file, and also import other style files */

@import 'tailwindcss';

:root {
  --primary-color: #ff5b00;
  --primary-dark: #d95700;
  --primary-light: #ff9a4d;
  --page-background: #f7f8fa;
  --section-background: #ffffff;
  --card-background: #ffffff;
  --text-primary: #15191e;
  --text-secondary: #626b75;
  --border-color: rgba(17, 24, 39, 0.1);
  --navbar-background: rgba(255, 255, 255, 0.9);
  --soft-background: #eef1f4;
  --card-shadow: 20px 50px rgba(15, 23, 42, 0.1);
  --card-shadow-hover: 30px 70px rgba(15, 23, 42, 0.18);
}

/* Dark Theme Variables */
body.dark-theme {
  --page-background: #0c1015;
  --section-background: #111820;
  --card-background: #161c24;
  --text-primary: #f4f7fa;
  --text-secondary: #aab4bf;
  --border-color: rgba(255, 255, 255, 0.1);
  --navbar-background: rgba(12, 16, 21, 0.9);
  --soft-background: #10161d;
  --card-shadow: 20px 50px rgba(0, 0, 0, 0.3);
  --card-shadow-hover: 30px 80px rgba(0, 0, 0, 0.5);
}

/* ==========================================================================
   2. Global Reset & Base Styles
   ========================================================================== */

*, 
*::before, 
*::after {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  font-family: "Segoe UI", Arial, sans-serif;
  color: var(--text-primary);
  background-color: var(--page-background);
  transition: background-color 0.35s ease, color 0.35s ease;
}

button, 
input, 
textarea, 
select {
  font-family: inherit;
}

button, 
a {
  cursor: pointer;
}

a {
  text-decoration: none;
  color: inherit; /* Keeps link colors natural to text flows */
}

img {
  display: block;
  max-width: 100%;
  height: auto;
}
