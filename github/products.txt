<!-- Products Hero -->

<section class="products-hero">

  <div class="hero-pattern"></div>

  <div class="hero-content">

    <span class="section-label">
      KHETAL PRODUCT CATALOGUE
    </span>

    <h1>
      Strength for every
      <span>structure.</span>
    </h1>

    <p>
      Explore our range of pipes, structural steel,
      plates, roofing materials and related products.
    </p>

    <div class="hero-statistics">

      <div>
        <strong>
          {{ productService.products().length }}
        </strong>

        <span>Products</span>
      </div>

      <div>
        <strong>
          {{ categories().length - 1 }}
        </strong>

        <span>Categories</span>
      </div>

      <div>
        <strong>25+</strong>
        <span>Years of experience</span>
      </div>

    </div>

  </div>

</section>

<!-- Search and Filters -->

<section class="catalogue-section">

  <div class="catalogue-heading">

    <div>

      <span class="section-label">
        OUR RANGE
      </span>

      <h2>
        Find the right steel product.
      </h2>

    </div>

    <div class="search-container">

      <span class="search-icon">⌕</span>

      <input
        #searchInput
        type="search"
        placeholder="Search products or categories"
        (input)="updateSearch(searchInput.value)"
      >

    </div>
    

  </div>

  <div class="category-filters">

    @for (
      category of categories();
      track category
    ) {

      <button
        type="button"
        [class.active]="
          selectedCategory() === category
        "
        (click)="selectCategory(category)"
      >
        {{ category }}
      </button>

    }

  </div>

  <!-- Product Cards -->

  <div class="product-grid">

    @for (
      product of filteredProducts();
      track product.id;
      let index = $index
    ) {

      <article
        class="product-card"
        [style.--animation-index]="index"
      >

        <div class="product-image">

          <img
            [src]="product.image"
            [alt]="product.name"
          >

          <div class="image-overlay"></div>

          <span class="category-badge">
            {{ product.category }}
          </span>

          @if (product.featured) {

            <span class="featured-badge">
              Featured
            </span>

          }

          <span
            class="availability"
            [class.unavailable]="!product.available"
          >
            <i></i>

            {{
              product.available
                ? 'Available'
                : 'Unavailable'
            }}
          </span>

        </div>

        <div class="product-information">

          <div class="product-number">
            PRODUCT
            {{
              (index + 1)
                .toString()
                .padStart(2, '0')
            }}
          </div>

          <h3>
            {{ product.name }}
          </h3>

          <p>
            {{ product.shortDescription }}
          </p>

          <button
            type="button"
            class="view-button"
            (click)="openProduct(product)"
          >
            View details
            <span>↗</span>
          </button>

        </div>

      </article>

    } @empty {

      <div class="empty-state">

        <div>⌕</div>

        <h3>No products found</h3>

        <p>
          Try another product name or category.
        </p>

        <button
          type="button"
          (click)="
            updateSearch('');
            selectCategory('All')
          "
        >
          Show all products
        </button>

      </div>

    }

  </div>

</section>

<!-- Quality Section -->

<section class="quality-section">

  <div class="quality-heading">

    <span class="section-label">
      KHETAL ADVANTAGE
    </span>

    <h2>
      More than products.
      <span>A dependable supply partner.</span>
    </h2>

  </div>

  <div class="quality-grid">

    <article>
      <span>01</span>
      <h3>Quality-focused stock</h3>
      <p>
        Products selected for dependable use across
        construction and industrial requirements.
      </p>
    </article>

    <article>
      <span>02</span>
      <h3>Competitive pricing</h3>
      <p>
        Material options provided with a focus on
        competitive and transparent commercial value.
      </p>
    </article>

    <article>
      <span>03</span>
      <h3>Timely service</h3>
      <p>
        Efficient customer support and order
        coordination for smoother fulfilment.
      </p>
    </article>

    <article>
      <span>04</span>
      <h3>Odd-size availability</h3>
      <p>
        Support for specifications that may not be
        readily available elsewhere.
      </p>
    </article>

  </div>

</section>

<!-- Product Details Modal -->
@if (selectedProduct(); as product) {
  <div class="product-modal-backdrop" (click)="closeProduct()">
    <article class="product-modal" (click)="$event.stopPropagation()">
      <button type="button" class="modal-close" (click)="closeProduct()" aria-label="Close product details">
        &times;
      </button>

      <div class="modal-image">
        <img [src]="product.image" [alt]="product.name">
      </div>

      <div class="modal-content">
        <span class="section-label">{{ product.category }}</span>
        <h2>{{ product.name }}</h2>
        <p>{{ product.description }}</p>

        <div class="modal-status">
          <span [class.unavailable-text]="!product.available">
            {{ product.available ? 'Currently available' : 'Currently unavailable' }}
          </span>
          @if (product.featured) {
            <span>Featured product</span>
          }
        </div>

        <a href="#contact">
          Send enquiry <span>&rarr;</span>
        </a>
      </div>
    </article>
  </div>
}





-------------------------------------------------------------------------



:host { 
    display: block; 
} 

/* ================= HERO ================= */ 
.products-hero { 
    min-height: 70vh; 
    display: flex; 
    justify-content: center; 
    align-items: center; 
    text-align: center; 
    color: #fff; 
    background: linear-gradient(rgba(0, 0, 0, .65), rgba(0, 0, 0, .65)), url('https://images.unsplash.com/photo-1565793298595-6a879b1d9492?w=1800'); 
    background-size: cover; 
    background-position: center; 
} 

.hero-content h1 { 
    font-size: 4rem; 
    margin: 20px 0; 
} 

.hero-content h1 span { 
    color: var(--primary-color); 
} 

.hero-content p { 
    max-width: 700px; 
    margin: auto; 
    line-height: 1.8; 
} 

/* ================= SEARCH ================= */ 

/* =========================================
   Search Section
========================================= */

.catalogue-section {
  padding: 60px 6%;
}

/* =========================================
   Section Heading
========================================= */

.catalogue-title{
  margin-bottom:40px;

  text-align:center;
}

.section-label{

  display:inline-block;

  padding:10px 18px;

  background:
  rgba(255,91,0,.08);

  color:var(--primary-color);

  border-radius:30px;

  font-size:13px;
  font-weight:700;

  letter-spacing:3px;

  margin-bottom:20px;

  text-transform:uppercase;
}

.catalogue-title h2{

  margin:0;

  color:var(--text-primary);

  font-size:
  clamp(2.8rem,5vw,4.5rem);

  line-height:1.1;

  font-weight:800;
}

.catalogue-title h2::after{

  content:"";

  display:block;

  width:90px;
  height:5px;

  margin:20px auto;

  border-radius:20px;

  background:
  linear-gradient(
    90deg,
    var(--primary-color),
    var(--primary-light)
  );
}

.catalogue-title p{

  max-width:700px;

  margin:20px auto 0;

  color:var(--text-secondary);

  font-size:1.05rem;

  line-height:1.9;
}

.search-container {
  position: relative;

  max-width: 750px;
  margin: auto;
}

.search-container input[type="search"] {
  width: 100%;

  padding: 18px 20px 18px 60px;

  border: 1px solid var(--border-color);

  border-radius: 60px;

  background: var(--card-background);

  color: var(--text-primary);

  font-size: 16px;

  outline: none;

  transition: all .3s ease;

  box-shadow:
    0 8px 24px rgba(0,0,0,.05);
}

.search-container input[type="search"]:focus {
  border-color: var(--primary-color);

  box-shadow:
    0 0 0 4px rgba(255,91,0,.12);
}

.search-icon {
  position: absolute;

  left: 22px;
  top: 50%;

  transform: translateY(-50%);

  color: var(--primary-color);

  font-size: 20px;

  pointer-events: none;
}

/* ================= FILTERS ================= */ 
/* =========================================
   Category Filters
========================================= */

.category-filters{
  display:flex;
  flex-wrap:wrap;
  justify-content:center;

  gap:12px;

  padding:30px 6%;
}

.category-filters button{

  padding:12px 22px;

  border:1px solid var(--border-color);

  border-radius:50px;

  background:var(--card-background);

  color:var(--text-primary);

  font-size:14px;
  font-weight:600;

  cursor:pointer;

  transition:.3s;
}

.category-filters button:hover{

  border-color:var(--primary-color);

  color:var(--primary-color);

  transform:translateY(-2px);
}

.category-filters button.active{

  background:var(--primary-color);

  color:white;

  border-color:var(--primary-color);
}

/* Dark Theme */

body.dark-theme .category-filters button{

  background:var(--card-background);

  color:var(--text-primary);
}

body.dark-theme .category-filters button.active{

  background:var(--primary-color);

  color:white;
}

/* Mobile */

@media(max-width:768px){

  .category-filters{

    justify-content:flex-start;

    overflow-x:auto;

    padding:20px 5%;
  }

  .category-filters button{

    white-space:nowrap;
  }
}
/* ================= PRODUCTS ================= */ 
.product-grid { 
    display: grid; 
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); 
    gap: 25px; 
} 

.product-card { 
    background: var(--card-background); 
    border: 1px solid var(--border-color); 
    border-radius: 20px; 
    overflow: hidden; 
    transition: .3s; 
} 

.product-card:hover { 
    transform: translateY(-8px); 
} 

.product-image { 
    position: relative; 
    height: 250px; 
} 

.product-image img { 
    width: 100%; 
    height: 100%; 
    object-fit: cover; 
} 

.category-badge, .featured-badge { 
    position: absolute; 
    top: 15px; 
    padding: 6px 12px; 
    border-radius: 20px; 
    color: #fff; 
    font-size: 12px; 
} 

.category-badge { 
    left: 15px; 
    background: var(--primary-color); 
} 

.featured-badge { 
    right: 15px; 
    background: #22c55e; 
} 

.availability { 
    position: absolute; 
    bottom: 15px; 
    left: 15px; 
    color: white; 
    font-size: 12px; 
} 

.product-information { 
    padding: 22px; 
} 

.product-number { 
    color: var(--primary-color); 
    font-size: 12px; 
    letter-spacing: 2px; 
} 

.product-information h3 { 
    margin: 10px 0; 
    color: var(--text-primary); 
} 

.product-information p { 
    color: var(--text-secondary); 
    line-height: 1.7; 
} 

.view-button { 
    margin-top: 15px; 
    border: none; 
    padding: 12px 20px; 
    border-radius: 30px; 
    background: var(--primary-color); 
    color: white; 
    cursor: pointer; 
} 

/* ================= EMPTY ================= */ 
.empty-state { 
    text-align: center; 
    padding: 60px 20px; 
} 

/* ================= QUALITY ================= */ 
.quality-section { 
    padding: 80px 6%; 
    background: var(--section-background); 
} 

.quality-heading { 
    text-align: center; 
    margin-bottom: 50px; 
} 

.quality-heading h2 { 
    color: var(--text-primary); 
} 

.quality-heading h2 span { 
    display: block; 
    color: var(--primary-color); 
} 

.quality-grid { 
    display: grid; 
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); 
    gap: 25px; 
} 

.quality-grid article { 
    padding: 25px; 
    border-radius: 20px; 
    background: var(--card-background); 
    border: 1px solid var(--border-color); 
    transition: .3s; 
} 

.quality-grid article:hover { 
    border-color: var(--primary-color); 
    transform: translateY(-5px); 
} 

.quality-grid span { 
    color: var(--primary-color); 
    font-weight: 700; 
} 

.quality-grid h3 { 
    color: var(--text-primary); 
} 

.quality-grid p { 
    color: var(--text-secondary); 
} 
/* =========================================
   Product Modal
========================================= */

.product-modal-backdrop{
  position:fixed;
  inset:0;

  display:flex;
  justify-content:center;
  align-items:center;

  padding:20px;

  background:rgba(0,0,0,.75);

  backdrop-filter:blur(6px);

  z-index:2000;
}

.product-modal{
  position:relative;

  width:min(950px,95%);
  max-height:90vh;

  overflow-y:auto;

  border-radius:24px;

  background:var(--card-background);

  border:1px solid var(--border-color);

  animation:modalOpen .3s ease;
}

/* Close Button */

.modal-close{
  position:absolute;

  top:15px;
  right:15px;

  width:42px;
  height:42px;

  border:none;

  border-radius:50%;

  background:rgba(0,0,0,.08);

  color:var(--text-primary);

  font-size:1.5rem;

  cursor:pointer;

  z-index:10;
}

.modal-close:hover{
  background:var(--primary-color);
  color:white;
}

/* Image */

.modal-image{
  height:350px;
  overflow:hidden;
}

.modal-image img{
  width:100%;
  height:100%;

  object-fit:cover;
}

/* Content */

.modal-content{
  padding:35px;
}

.modal-content h2{
  margin:15px 0;

  color:var(--text-primary);

  font-size:2rem;
}

.modal-content p{
  color:var(--text-secondary);

  line-height:1.8;
}

/* Status */

.modal-status{
  display:flex;
  gap:15px;

  flex-wrap:wrap;

  margin:25px 0;
}

.modal-status span{
  padding:8px 14px;

  border-radius:30px;

  background:var(--soft-background);

  color:var(--text-primary);

  font-size:.9rem;
}

.unavailable-text{
  color:#ef4444 !important;
}

/* Enquiry Button */

.modal-content a{
  display:inline-flex;
  align-items:center;
  gap:8px;

  padding:14px 24px;

  border-radius:50px;

  background:var(--primary-color);

  color:white;

  text-decoration:none;

  font-weight:600;

  transition:.3s;
}

.modal-content a:hover{
  background:var(--primary-dark);

  transform:translateY(-2px);
}

/* Animation */

@keyframes modalOpen{

  from{
    opacity:0;
    transform:translateY(20px);
  }

  to{
    opacity:1;
    transform:translateY(0);
  }
}

/* Mobile */

@media(max-width:768px){

  .modal-image{
    height:220px;
  }

  .modal-content{
    padding:25px;
  }

  .modal-content h2{
    font-size:1.6rem;
  }
}










----------------------------------------------------------------------





import {
  Component,
  computed,
  signal
} from '@angular/core';

import {
  FormsModule
} from '@angular/forms';

import {
  Product
} from '../../models/product';

import {
  ProductService
} from '../../services/product';

@Component({
  selector: 'app-products',
  standalone: true,
  imports: [
    FormsModule
  ],
  templateUrl: './products.html',
  styleUrl: './products.css'
})
export class ProductsComponent {

  searchText = signal('');
  selectedCategory = signal('All');
  selectedProduct = signal<Product | null>(null);

  readonly categories = computed(() => {
    const productCategories =
      this.productService
        .products()
        .map(product => product.category);

    return [
      'All',
      ...new Set(productCategories)
    ];
  });

  readonly filteredProducts = computed(() => {

    const search =
      this.searchText().trim().toLowerCase();

    const category =
      this.selectedCategory();

    return this.productService
      .products()
      .filter(product => {

        const matchesSearch =
          product.name.toLowerCase().includes(search) ||
          product.category.toLowerCase().includes(search) ||
          product.shortDescription
            .toLowerCase()
            .includes(search);

        const matchesCategory =
          category === 'All' ||
          product.category === category;

        return matchesSearch && matchesCategory;
      });
  });

  constructor(
    public productService: ProductService
  ) {}

  updateSearch(value: string): void {
    this.searchText.set(value);
  }

  selectCategory(category: string): void {
    this.selectedCategory.set(category);
  }

  openProduct(product: Product): void {
    this.selectedProduct.set(product);
  }

  closeProduct(): void {
    this.selectedProduct.set(null);
  }
}
``
